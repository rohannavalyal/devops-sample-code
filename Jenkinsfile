pipeline {
    agent any
    stages {
        stage('Hello World') {
            steps {
                sh 'chmod +x hello-world.sh'
                sh './hello-world.sh'
            }
        }
    }
}
